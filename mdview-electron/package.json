{
  "name": "mdview-electron",
  "version": "0.1.0",
  "description": "Markdown viewer powered by mdview-core WASM",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:wasm": "wasm-pack build ../mdview-core --target web --features wasm --out-dir wasm",
    "dev": "npm run build:wasm && electron ."
  },
  "dependencies": {
    "electron": "^34.0.0",
    "highlight.js": "^11.11.1"
  }
}
